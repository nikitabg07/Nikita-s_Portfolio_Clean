[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"

# Redirects all URLs to index.html for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
